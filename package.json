{
  "name": "azusa3-marp-theme",
  "description": "Azusa 3 theme for Marp",
  "version": "1.0.0",
  "author": "zerosum/TAKAHASHI Osamu",
  "license": "MIT",
  "devDependencies": {
    "highlight.js": "^11.3.1",
    "rimraf": "^3.0.2",
    "sass": "^1.43.4"
  },
  "scripts": {
    "clean": "rimraf build",
    "watch": "sass --watch --no-source-map src/sass:build/css",
    "build": "sass --no-source-map --style=compressed src/sass:build/css"
  }
}
